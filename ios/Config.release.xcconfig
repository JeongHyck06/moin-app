// CocoaPods 설정 위에 .env 값을 얹는다
// tmp.xcconfig 는 스킴 pre-action 이 ENVFILE 을 읽어 빌드마다 새로 만들고 gitignore 대상
// Xcode 에서 직접 빌드하면 ENVFILE 이 없어 값이 비니 npm run ios (또는 ios:prod) 로 빌드할 것
#include "Pods/Target Support Files/Pods-Moin/Pods-Moin.release.xcconfig"
#include? "tmp.xcconfig"
